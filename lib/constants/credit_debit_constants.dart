import 'package:flutter/material.dart';

const Color creditColor = Color(0xFFFF0000);

const Color debitColor = Color(0xFF00FF00);

const String creditSelection = 'CREDIT';

const String debitSelection = 'DEBIT';

const entryContainerDecoration = BoxDecoration(
  color: Colors.white70,
  borderRadius: BorderRadius.all(
    Radius.circular(20.0),
  ),
);

const entryTextDecoration = InputDecoration(
  border: OutlineInputBorder(),
  fillColor: Color(0x4D000000),
);
