import 'package:flutter/material.dart';

const textStyle =
    TextStyle(fontSize: 30.0, fontWeight: FontWeight.w800, color: Colors.teal);

const margins = EdgeInsets.only(left: 15.0, right: 15.0, bottom: 10.0);

const shape = RoundedRectangleBorder(
  borderRadius: BorderRadius.all(Radius.circular(20.0)),
  side: BorderSide(width: 1.0),
);

const shadowColor= Color(0x80808080);

const containerColor= Color(0xFFFFFFFF);
